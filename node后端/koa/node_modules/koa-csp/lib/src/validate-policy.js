(function (factory) {
    if (typeof module === "object" && typeof module.exports === "object") {
        var v = factory(require, exports);
        if (v !== undefined) module.exports = v;
    }
    else if (typeof define === "function" && define.amd) {
        define(["require", "exports", "./directives", "./log", "./to-camel-case"], factory);
    }
})(function (require, exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var directives_1 = require("./directives");
    var log = require("./log");
    var to_camel_case_1 = require("./to-camel-case");
    var directivesWithCamelCase = directives_1.default.concat(directives_1.default.map(to_camel_case_1.default));
    function default_1(policy) {
        var keys = Object.keys(policy);
        var valid = true;
        if (!keys.length) {
            log.warn('Empty Policy');
            valid = false;
        }
        var invalidDirectives = keys
            .filter(function (key) { return !directivesWithCamelCase.includes(key); });
        if (invalidDirectives.length) {
            log.warn("Invalid Policy Name: " + invalidDirectives.join(', '));
            valid = false;
        }
        return valid;
    }
    exports.default = default_1;
});
